{% extends 'layouts/base.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto mt-5">
            <h1 class="mt5 mb-3 text-center">Reset your password</h1>

            {% for flashError in app.flashes('reset_password_error') %}
                <div class="alert alert-error mb-3 rounded" role="alert">{{ flashError }}</div>
            {% endfor %}

           <p class="mb-2 text-center">
               Entrez votre email, afin de vous envoyez le lien de réinitialisation du MDP.
           </p>

            {{ form_start(requestForm) }}
            {{ form_row(requestForm.email) }}


            <button type="submit" class="btn btn-primary btn-block p-3">Envoyer</button>
            {{ form_end(requestForm) }}

        </div>
    </div>




{% endblock %}
