{% extends 'base.html.twig' %}

{% block title 'Panterest'%}

{% block body %}
<h1>{{ pluralize(pins|length, 'pin') }} </h1>
    {% for pin in pins %}
        <article>
            <h1>{{ pin.title }}</h1>
            <p>{{ pin.description }}</p>
        </article>
    {% endfor %}
{% endblock %}
